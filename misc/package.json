{
  "name": "misc",
  "version": "1.0.0",
  "description": "Build the questionBank.js file used in various places",
  "dependencies": {
    "csv-parser": "^3.0.0"
  },
  "scripts": {
    "start": "node createQuestionBank.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}